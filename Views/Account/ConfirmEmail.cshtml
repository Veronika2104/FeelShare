@model bool
@{
    ViewData["Title"] = "Подтверждение email";
}
@if (Model)
{
    <h2>Email подтверждён 🎉</h2>
    <p>Добро пожаловать в FeelShare.</p>
    <p><a class="btn btn-primary" href="@(ViewBag.ReturnUrl ?? "/me")">Продолжить</a></p>
}
else
{
    <h2>Не удалось подтвердить email</h2>
    <p>Ссылка недействительна или уже использована. Запросите новую в форме ниже.</p>
    <p><a class="btn btn-outline-primary" asp-action="ResendConfirmation">Отправить письмо ещё раз</a></p>
}